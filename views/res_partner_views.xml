<?xml version="1.0" encoding="utf-8"?>
<!-- 聯絡人 form：LINE 整合分頁已移除，改用 LINE Bridge 的 smart button -->
<odoo>
    <record id="res_partner_view_form_line" model="ir.ui.view">
        <field name="name">res.partner.form.line</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <!-- intentionally empty: LINE tab removed, use smart button instead -->
            </xpath>
        </field>
    </record>
</odoo>
